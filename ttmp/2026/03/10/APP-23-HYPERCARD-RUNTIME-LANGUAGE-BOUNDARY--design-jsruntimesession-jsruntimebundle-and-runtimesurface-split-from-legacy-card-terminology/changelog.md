# Changelog

## 2026-03-10

- Initial workspace created
- Added APP-23 as the design ticket for splitting language-specific runtime concepts from the legacy overloaded `card` terminology.
- Documented that the current QuickJS engine is not itself card-bound, but the transport, bootstrap, host, and desktop APIs are.
- Compared the current model against `vm-system` and used that as precedent for a clearer runtime-first lower layer.
- Revised the ticket after further architecture clarification: the right top-level model is not just session plus surface, but `RuntimeSession`, `RuntimeBundle`, `RuntimePackage`, `RuntimeSurface`, and `RuntimeSurfaceType`.
- Explicitly treated `ui` and `kanban` as installable runtime packages, and clarified that current `RuntimePack` behavior is really a surface-type registry rather than the whole package concept.
- Dropped the earlier `JSRuntimeSession` / `JSRuntimeBundle` naming so the immediate rename stays focused on the current system rather than pre-optimizing for future multi-language work.
- Chose a direct-cutover rename strategy instead of compatibility aliases.
- Expanded APP-23 with a detailed execution backlog broken into direct-cutover implementation slices covering runtime transport, bootstrap globals, package registry introduction, host/session renames, desktop/windowing type cleanup, docs/prompt alignment, app migration, and validation.
- Completed the first implementation slice by cutting over the host-side runtime surface-type registry: `runtimePackRegistry` became `runtimeSurfaceTypeRegistry`, the public registry API now uses `RuntimeSurfaceType*` nouns, and dependent tests/host imports were updated without adding aliases (`go-go-os-frontend` `d3d26a3`, `wesen-os` `025da34`).
- Completed Slice 2 by renaming the QuickJS runtime transport and service API to bundle/surface nouns: `QuickJSCardRuntimeService` became `QuickJSRuntimeService`, transport payloads now use `surfaceId`, `surfaces`, `surfaceTypes`, and `initialSurfaceState`, and the old method names were deleted instead of aliased (`go-go-os-frontend` `02ad2ad`).
- Completed Slice 3 by renaming the VM bootstrap and built-in authoring globals in one cut: `defineStackBundle` became `defineRuntimeBundle`, `defineCard*` became `defineRuntimeSurface*`, `__stackHost` became `__runtimeBundleHost`, and all touched built-in VM bundles plus `vmmeta` generation were updated together (`go-go-os-backend` `091e56f`, `go-go-app-inventory` `6da5f43`, `go-go-app-arc-agi-3` `3dd7f8f`, `go-go-app-sqlite` `f90b945`, `wesen-os` `e0d20e8`).
- Validated the Slice 2/3 cutover with targeted runtime tests, `hypercard-runtime` typecheck, `vmmeta` generation for `os-launcher` and `inventory`, and targeted bundle tests in both apps.
- Completed Slice 4 by introducing an explicit runtime package registry and pre-install path: `ui` and `kanban` now register as `RuntimePackageDefinition`s with VM install preludes, bundles declare `packageIds`, the runtime installs packages before evaluating bundle code, and bundle metadata now records `packageIds` for exact-match validation (`go-go-os-frontend` `3bfff72`, `go-go-app-inventory` `dba9519`, `go-go-app-sqlite` `5c467e2`, `go-go-app-arc-agi-3` `f1eb352`, `wesen-os` `8b9718a`).
- Added direct tests for package ordering, unknown-package failure, and bundle-package mismatch, and updated app/story/test stacks to require explicit `plugin.packageIds`.
- Validation for Slice 4 passed with targeted `hypercard-runtime` tests, targeted engine/windowing tests, `hypercard-runtime` typecheck, `os-launcher` and `inventory` bundle tests, plus `vmmeta` regeneration; a full `apps/os-launcher` TypeScript build still reports pre-existing linked `rich-widgets` errors unrelated to this slice.
- Completed the first Slice 5 pass by auditing the remaining bundle preludes and confirming they hold app-local helper composition rather than hidden public DSL; `os-launcher` and `inventory` bundle assembly now explicitly separates bundle-local prelude, docs, and surface sources, and the prelude files themselves state that public DSL belongs to runtime packages (`go-go-app-inventory` `ae6ecb4`, `wesen-os` `f38037a`).
- Completed the first pass of Slice 6 by renaming the live runtime host and injected-definition registry to surface nouns: `PluginCardSessionHost` became `RuntimeSurfaceSessionHost`, `runtimeCardRegistry` became `runtimeSurfaceRegistry`, direct launcher consumers were updated in `os-launcher`, `inventory`, `sqlite`, and `arc-agi-player`, and the focused runtime tests/typecheck stayed green after the cutover (`go-go-os-frontend` `4e65f33`, `wesen-os` `588d09e`, `go-go-app-inventory` `757b380`, `go-go-app-sqlite` `1f8a572`, `go-go-app-arc-agi-3` `aec8245`).
- Validation for this Slice 6 checkpoint passed in `hypercard-runtime`, `os-launcher`, and inventory; SQLite launcher tests still hit a pre-existing `createAction` runtime import problem, and ARC targeted `tsc` still fails with the existing linked-workspace `rootDir` / `?raw` errors unrelated to the host rename.
- Completed the second Slice 6 checkpoint by renaming the runtime Redux/session layer and editor provenance helpers to the final nouns: the `pluginCardRuntime` feature became `runtimeSessions`, selector/state names now use `surfaceState`, `surfaceId`, and `runtimeSessionsReducer`, the editor provenance file became `runtimeSurfaceRef`, and downstream consumers such as `apps-browser`, `hypercard-tools`, and ARC store/bridge code were updated in the same cutover (`go-go-os-frontend` `0a09895`, `go-go-app-arc-agi-3` `052cacf`).
- Validation for the second Slice 6 checkpoint passed with targeted `runtime-sessions`, `plugin-intent-routing`, editor-ref, runtime-host, debug-window, and runtime-service tests; `hypercard-runtime` typecheck passed; `apps/apps-browser` TypeScript validation passed; `os-launcher` launcher tests passed; inventory app `tsc` passed; and the only remaining noisy validations stayed the same existing SQLite/ARC issues already called out above.
- Completed Slice 7 by renaming the desktop/windowing and launcher-facing runtime nouns to `bundle` and `surface`: `CardStackDefinition` became `RuntimeBundleDefinition`, `CardDefinition` became `RuntimeSurfaceMeta`, window content now uses `kind: 'surface'`, session-nav state now stores `surface`, and the windowing shell/tests/stories plus runtime debug tooling moved to the new payload shape without preserving the old `card` content kind (`go-go-os-frontend` `e042f41`, `wesen-os` `a544136`, `go-go-app-inventory` `d67feaf`, `go-go-app-sqlite` `30caa5d`, `go-go-app-arc-agi-3` `eaafe5d`).
- As part of the same slice, updated the remaining first-party launcher stacks and adapters in `inventory`, `sqlite`, `arc-agi-player`, `book-tracker-debug`, `crm`, `hypercard-tools`, `todo`, and `os-launcher` so they open runtime surfaces instead of card windows.
- Fixed the SQLite launcher test harness debt that had been hiding rename fallout: the local Vitest `@reduxjs/toolkit` stub now exports `createAction`, so the launcher suite can execute instead of crashing during module import (`go-go-app-sqlite` `aca1074`).
- Validation for Slice 7 passed with targeted desktop/windowing tests, runtime debug/runtime-host tests, `hypercard-runtime` typecheck, `apps/apps-browser` TypeScript validation, `os-launcher` launcher tests, `inventory` app typecheck, and `sqlite` launcher tests.
- Completed the first Slice 8 pass by aligning live authoring sources and docs with the new surface navigation contract: `os-launcher` and inventory VM sources now dispatch `nav.go` with `surfaceId`, the shared nav fixture/tests were updated the same way, `runtime-card-policy.md` now teaches runtime surfaces rather than runtime cards, and regenerated `vmmeta` outputs now carry the updated package/surface phrasing (`go-go-os-frontend` `9be1afd`, `go-go-app-inventory` `a36f99d`, `wesen-os` `d5f8a27`).
- Validation for this Slice 8 checkpoint passed with `runtime-sessions` tests in `hypercard-runtime`, `vmmeta` generation plus `pluginBundle.test.ts` in `os-launcher`, and `vmmeta` generation plus inventory typecheck/chat-render tests in `go-go-app-inventory`.
- Completed the first Slice 9 checkpoint by renaming the artifact/debug/editor boundary to runtime-surface nouns: artifact state now stores `runtimeSurfaceId` / `runtimeSurfaceCode`, injection result payloads use `surfaceId`, the runtime surface registry no longer leaks `cardId`, the debug window reads artifact and registry state as surfaces, and the HyperCard timeline renderer opens/edit surfaces using the renamed metadata (`go-go-os-frontend` pending commit, `go-go-app-inventory` pending commit).
- As part of the same checkpoint, updated the remaining inventory chat projection assertions so the inventory host store expects `runtimeSurfaceId` after artifact projection instead of the old `runtimeCardId`.
- Validation for the Slice 9 checkpoint passed with focused artifact/runtime/debug tests in `hypercard-runtime`, targeted inventory chat projection tests, and `hypercard-runtime` typecheck.
- Completed the second Slice 9 checkpoint by renaming the shared debug window symbol and file to `RuntimeSurfaceDebugWindow`, updating the package barrel/runtime debug app import path, and keeping the focused debug/timeline/artifact tests green after the move (`go-go-os-frontend` pending commit).
- Completed a small Slice 10 cleanup checkpoint by updating first-party stories, tests, and docs-browser fixture text to teach `surface` vocabulary instead of `card` where the runtime contract had already changed: `RuntimeMutation.stories.tsx` now uses `surfaceId`/`surfaces`, the runtime-session rerender test no longer names the render target `_cardId`, `os-launcher` test descriptions now talk about runtime surfaces, and apps-browser docs-mount fixtures now use `defineRuntimeSurface(...)` in example source text (`go-go-os-frontend` pending commit, `wesen-os` pending commit).
- Validation for this cleanup checkpoint passed in `apps-browser` and `hypercard-runtime` targeted tests; `apps/os-launcher` still shows the same intermittent Vitest stall after printing the bundle test result, so I recorded that as environment noise rather than claiming a clean launcher-host pass.
- Completed the Slice 9 doc-mount registration cutover by renaming the apps-browser `vmmeta` mounts from `pack/card` to `surface-type/surface`: mount factories, object kinds, canonical docs paths, `wesen-os` startup registration, and apps-browser tests/stories now use `/docs/objects/surface-type/...` and `/docs/objects/surface/...` instead of encoding the legacy nouns in the mounted-object tree (`go-go-os-frontend` pending commit, `wesen-os` pending commit).
- Validation for the doc-mount cutover passed with targeted `apps-browser` mount/catalog/detail tests, `apps/apps-browser` TypeScript validation, and a clean `apps/os-launcher` `launcherHost.test.tsx` pass after the startup registration rename.
- Closed the last remaining Slice 9 verification item by extending `RuntimeSurfaceDebugWindow.test.tsx` to assert that built-in predefined surfaces still expose source-backed `Edit` actions after the rename, alongside the already-covered “only the active surface appears in session actions” behavior (`go-go-os-frontend` pending commit).
- Continued the Slice 10 first-party app sweep by cutting the remaining VM-authored navigation helpers over from `nav.go { cardId }` to `nav.go { surfaceId }` in the SQLite, Todo, Book Tracker, CRM, and Hypercard Tools bundles. This was a real behavior fix, not just wording cleanup, because the host router now ignores `nav.go` payloads that still use `cardId` (`go-go-os-frontend` pending commit, `go-go-app-sqlite` pending commit).
- While validating the SQLite bundle sweep, found and fixed a second app-local test harness bug: the SQLite Vitest `@reduxjs/toolkit` stub now attaches `.type` to `createAction(...)` results and resolves `builder.addCase(actionCreator, ...)` against that `.type`, which makes the runtime-state queue tests behave like real RTK instead of silently dropping `extraReducers` (`go-go-app-sqlite` `661ca26`).
- Validation for this checkpoint passed with the SQLite launcher/runtime-state test suite, `hypercard-tools` app TypeScript validation, and direct parse checks for the edited VM bundles; broader Todo/Book Tracker/CRM app `tsc` runs still report the pre-existing Storybook `CardStoriesConfig` mismatch unrelated to this navigation payload cutover.
- Cleaned the last live-source `homeCard` and `__stackBundle` stragglers in bundle assembly/bootstrap internals: `os-launcher` and inventory raw-source imports now use `homeSurface`, the bootstrap stores its loaded program in `__runtimeBundle`, and the remaining timeline test helper now uses a neutral `renderEntity(...)` name instead of reintroducing `renderCard(...)` inside an APP-23 test (`go-go-os-frontend` pending commit, `wesen-os` pending commit, `go-go-app-inventory` pending commit).
- Re-ran focused runtime/bootstrap, bundle-generation, and inventory/os-launcher bundle tests after that cleanup; the remaining repo-wide search hits are now concentrated in historical docs, archived ticket material, and legitimate `RuntimePackage*` symbols rather than live runtime-core `card` terminology.
- Closed the remaining Slice 10 examples/stories fixture gap: the engine demo VM fixtures (`DesktopShell.demo.vm.js`, `BookTracker.plugin.vm.js`) now dispatch `nav.go` with `surfaceId`, and the Todo, Book Tracker, and CRM story files now use the renamed `createStoryHelpers({ bundle, surfaceParams })` config shape, which removes the stale `CardStoriesConfig` type mismatch from those app-level TypeScript projects (`go-go-os-frontend` pending commit).
- Validation for that slice passed with `tsc --noEmit` for `apps/todo`, `apps/book-tracker-debug`, and `apps/crm`, plus parse checks for the edited engine demo VM sources.
- Started the public-docs cleanup slice by correcting the two most misleading live references: `go-go-os-frontend/docs/js-api-user-guide-reference.md` now teaches `RuntimeBundleDefinition`, `RuntimeSurfaceMeta`, `nav.go { surfaceId }`, and the current `createStoryHelpers({ bundle, surfaceParams })` API, and the inventory VM boundary doc now uses `renderRuntimeSurface(...)`, `eventRuntimeSurface(...)`, and `nav.go { surfaceId }` in its runtime examples (`go-go-os-frontend` pending commit, `go-go-app-inventory` pending commit).
- This docs pass is intentionally partial: it fixes the currently shipped/live reference pages that still actively taught the old runtime-core model, but it does not yet attempt to rewrite the broader historical ticket/archive docs that still mention `CardStackDefinition`, `homeCard`, or `renderCard(...)`.
- Made the artifact-boundary carve-out explicit: `hypercard.card.v2`, backend extractor/event names, and `HypercardCardRenderer` still remain card-shaped because the external artifact/timeline protocol is still card-shaped, but runtime-core internals should only keep `card` where they are actually parsing that envelope. As a small supporting cleanup, the timeline renderer helper names now describe artifact-envelope parsing instead of generic runtime-card state (`go-go-os-frontend` pending commit).
- Validated the ticket with `docmgr doctor` and uploaded the bundle to reMarkable at `/ai/2026/03/10/APP-23-HYPERCARD-RUNTIME-LANGUAGE-BOUNDARY`.

## 2026-03-11

- Finished the remaining APP-23 sweep by cleaning the last live runtime-core wording in the inventory VM-boundary guide and in focused runtime/inventory test descriptions, while keeping the intentional artifact-protocol `card` carve-out intact
- Added a repo-level runtime concepts guide at `go-go-os-frontend/docs/runtime-concepts-guide.md` so the final package/bundle/surface model is documented outside ticket docs
- Re-ran the focused runtime timeline test, inventory bundle test, and inventory `pkg/pinoweb` extractor/prompt tests to confirm the final wording cleanup did not hide a runtime regression
- Marked APP-23 complete: remaining `card` terms in the live tree are now either external artifact/protocol names, prompt/backend filenames, or historical ticket/archive material rather than runtime-core architecture leakage
