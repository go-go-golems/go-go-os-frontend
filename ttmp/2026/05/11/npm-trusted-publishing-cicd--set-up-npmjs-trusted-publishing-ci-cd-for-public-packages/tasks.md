# Tasks

## TODO

- [x] Create docmgr ticket for npmjs Trusted Publishing CI/CD.
- [x] Configure npmjs Trusted Publisher connections for current public packages.
- [ ] Add `.github/workflows/publish-npm.yml` with OIDC permissions and `npm-production` environment.
- [ ] Add or adapt a publish helper for package-set resolution and npmjs publication.
- [ ] Run dry-run workflow for a single package.
- [ ] Perform first real trusted-publishing patch release for a low-risk package.
- [ ] Document the release runbook after the first successful publish.
